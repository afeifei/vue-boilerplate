const config = {
  errorBagName: 'errors', // change if property conflicts.
  delay: 100,
  locale: 'en',
  messages: null,
  strict: true, // 没有设置规则的表单不进行校验
  events: 'blur', //默认是input
}

export default config;
